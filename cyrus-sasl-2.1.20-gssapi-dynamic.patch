We still want a dynamic gssapi plugin.

--- cyrus-sasl/configure.in	2004-10-24 16:05:13.000000000 -0400
+++ cyrus-sasl/configure.in	2004-10-27 19:58:01.223935096 -0400
@@ -486,7 +486,11 @@
 SASL_GSSAPI_CHK
 
 if test "$gssapi" != "no"; then
-  AC_DEFINE(STATIC_GSSAPIV2,[],[Link GSSAPI Staticly])
+  SASL_MECHS="$SASL_MECHS libgssapiv2.la"
+  if test "$enable_static" = yes; then
+    SASL_STATIC_OBJS="$SASL_STATIC_OBJS ../plugins/gssapi.o"
+    AC_DEFINE(STATIC_GSSAPIV2,[],[Link GSSAPI Staticly])
+  fi
   mutex_default="no"
   if test "$gss_impl" = "mit"; then
      mutex_default="yes"
